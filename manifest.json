{
  "manifest_version": 3,
  "name": "Tab Buster",
  "version": "0.1.0",
  "description": "Replace the new tab page with Tab Buster, a dashboard of open tabs grouped by friendly domain cards.",
  "permissions": ["tabs", "favicon"],
  "chrome_url_overrides": {
    "newtab": "newtab.html"
  }
}
